apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-pod
  labels:
    app: nginx
    types: front-end
spec:
  replicas: 3
  matchLabels:
    app: nginx
  templates:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
